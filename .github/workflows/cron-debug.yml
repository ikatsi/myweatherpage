name: cron-debug

on:
  workflow_dispatch:
  schedule:
    - cron: "*/5 * * * *"

permissions:
  contents: read

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Show time
        run: |
          echo "UTC now: $(date -u)"
          echo "Local now: $(date)"
